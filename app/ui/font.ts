import  { Inter, Lusitana, EB_Garamond } from 'next/font/google';

export const inter = Inter({ subsets: ['latin'] });

export const lusitana = Lusitana({ subsets: ['latin'], weight: [
    '400', '700'] });

export const ebgaramond = EB_Garamond({ subsets: ['latin'], weight: [
    '400', '700'] });